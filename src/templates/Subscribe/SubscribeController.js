import angular from 'angular'

angular
.module('petApp')
.controller('SubscribeController', SubscribeController)

SubscribeController.$inject = ['$scope'];

function SubscribeController ($scope) {
    
}